# CNN_Examples
Some Examples for CNNs

cnn_V8.3.py = First layers are AlexNet convolutional layers, for prediction I added just one fully connected layer.

cnn_V8.4.py = conv -> pool -> conv -> pool -> conv -> pool -> fully connected 
